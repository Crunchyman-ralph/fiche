{
  "name": "@fiche/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@fiche/db": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "better-auth": "^1.2.0",
    "elysia": "^1.2.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@fiche/tsconfig": "workspace:*",
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
